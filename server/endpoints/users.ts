type UserSettings = {
    phone: string,
    poemSettings: {
        poemAdjective: string,
        words: string[],
    },
    hour: number,
}

export class UserRepository {
    // login
    // signup
    // check token
    // set preferences
    // list current preferences 
}